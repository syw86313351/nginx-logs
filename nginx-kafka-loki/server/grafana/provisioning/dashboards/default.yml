apiVersion: 1
providers:
  - name: nginx-dashboards
    orgId: 1
    folder: Nginx
    folderUid: nginx
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
